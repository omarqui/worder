import { render, screen } from '@testing-library/react';
import App from './App';

test('renders Meaning Search', () => {
  // render(<App />);
  // const linkElement = screen.getByText(/Meaning Search/i);
  // expect(linkElement).toBeInTheDocument();
});
